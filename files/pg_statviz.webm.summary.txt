

PGSTATV is a new Postgres extension and utility pair which provides users with a way to analyze and visualize the cumulative and dynamic statistics stored in Postgres in order to gain insights into performance and troubleshoot issues. It is minimalist, modular, unobtrusive and requires minimal setup. It can produce visualizations of snapshots taken at intervals such as every 10 seconds. The source code for the extension and utility is available on GitHub.