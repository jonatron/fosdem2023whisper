 

In his talk, Mark presented Dagger, a portable CI-CD solution that helps teams avoid vendor locking and easily debug CI issues. Dagger allows developers to build pipelines with any language that can talk to a GraphQL API, is run from any environment with Docker installed, and offers caching automatic caching for all steps in the pipeline. He also discussed some upcoming improvements for secret management and extensions for general use.